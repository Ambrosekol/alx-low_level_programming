#include "main.h"
/**
  * _strcmp - compares two strings
  * @s1: string 1
  * @s2: string 2
  * Return: returns an Intiger
  */
int _strcmp(char *s1, char *s2)
{
	int a;

	a = strcmp(s1, s2);
	return (a);
}
