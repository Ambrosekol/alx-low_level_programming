#include <stdio.h>

/**
  * main - Starting point of the code
  *
  * Return: Always 0 (Success)
  */
int main(void)
{
	int ca;

	for (ca = '0'; ca <= '9'; ca++)
	{
		putchar(ca);
	}
	putchar('\n');

	return (0);
}
